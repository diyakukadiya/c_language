	  #include<stdio.h>
	  #include<conio.h>
	  main()
	  {
	   int a;
	   int b;
	   clrscr();
	   printf("Enter the value of a:");
	   scanf("%d",&a);
	   printf("Enter the value of b:");
	   scanf("%d",&b);
	   if(a<b)
	   {
	  printf("the first number is minimum numbere:");
	  }
	  else
	  {
	  printf("the second number is minimum number:");
	  }
	  getch();
	  }

